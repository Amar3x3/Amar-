#include<stdio.h>
#include<conio.h>
int main(){
    int cap;
    printf("enter number:-");
    scanf("%d",&cap);
    for (int i = 0; i <=cap; i++)
    {
        if (/* condition */)
        {
            /* code */
        }
        
    }
    
}